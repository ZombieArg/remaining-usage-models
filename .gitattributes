# Normalise line endings so contributors on macOS and Linux do not produce
# whole-file diffs against a Windows checkout.
* text=auto eol=lf

# Windows script hosts are happiest with CRLF.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

*.png binary
*.ico binary
*.exe binary
